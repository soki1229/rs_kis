mod error;
// mod environment;  // disabled: uses dotenv (removed from deps in redesign)
// mod websockets;   // disabled: uses lazy_static (removed from deps in redesign)
// mod logger;       // disabled: uses env_logger (removed from deps in redesign)
// mod api;          // disabled: depends on environment/logger modules
// mod core;
// mod types;
// mod client;       // disabled: depends on api/environment modules
// mod extentions;

// pub use client::KisClient;